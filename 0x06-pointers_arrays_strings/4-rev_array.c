#include "main.h"

/**
 * reverse_array - reverses a string
 * @a: first input
 * @n: second input
**/

void reverse_array(int *a, int n)
{
	int i;
	int j = 0;

	for (i = n; i < 0; i--)
	{
		a[j] == a[i];
		j++;
	}
}
